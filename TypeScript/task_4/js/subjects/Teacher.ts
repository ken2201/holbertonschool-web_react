// Define a namespace called Subjects to group related interfaces and classes
namespace Subjects {
    // Define an interface for a Teacher with properties for first name and last name
    export interface TeacherInterface {
        firstName: string; // Property for the teacher's first name
        lastName: string;  // Property for the teacher's last name
    }
}
